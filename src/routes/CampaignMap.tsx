/**
 * CampaignMap - Future feature: Boss selection and progression visualization
 * Currently inaccessible stub
 */

import { viewMessageClasses, viewPanelClasses, viewShellClasses, viewTitleClasses } from '../styles/uiClasses';

export function CampaignMap() {
  return (
    <main className={viewShellClasses}>
      <section className={viewPanelClasses}>
        <h1 className={viewTitleClasses}>Campaign Map</h1>
        <p className={viewMessageClasses}>Coming Soon: Boss selection and campaign progression</p>
      </section>
    </main>
  );
}
