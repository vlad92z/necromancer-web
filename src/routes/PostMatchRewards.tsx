/**
 * PostMatchRewards - Future feature: Deck improvements and rewards after winning
 * Currently inaccessible stub
 */

import { viewMessageClasses, viewPanelClasses, viewShellClasses, viewTitleClasses } from '../styles/uiClasses';

export function PostMatchRewards() {
  return (
    <main className={viewShellClasses}>
      <section className={viewPanelClasses}>
        <h1 className={viewTitleClasses}>Post-Match Rewards</h1>
        <p className={viewMessageClasses}>Coming Soon: Collect rewards and improve your deck</p>
      </section>
    </main>
  );
}
