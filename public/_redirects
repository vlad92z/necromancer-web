# SPA fallback: serve index.html for client-side routes
# Explicit rules first to avoid Cloudflare Pages canonicalization creating a loop
/index.html  /index.html 200
/index       /index.html 200
/*           /index.html 200
